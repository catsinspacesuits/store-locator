<%= map(:center => {
    :latlng => [52.50504797921441, 13.4080235665359283],
    :zoom => 12
  },
  markers: @stores.map { |store| { latlng: store.location.split(/\s*,\s*/), 
  # expects a string  
  popup: store.name + ", " + store.location } } 
  
) %>

<main>
  <h2>Spätis</h2>
  <% @stores.each do |store| %>
      <p><%= link_to store.name, store_path(store) %></p>
  <% end %>
</main>
